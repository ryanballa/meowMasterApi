'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var rg = require('regexparam');
var url = require('url');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var rg__default = /*#__PURE__*/_interopDefaultLegacy(rg);

const rgExec = (path, result) => {
    let i = 0;
    const out = {};
    const matches = result.pattern.exec(path);
    while (i < result.keys.length) {
        out[result.keys[i]] = (matches === null || matches === void 0 ? void 0 : matches[++i]) || null;
    }
    return out;
};
const getURLParams = (reqUrl = '/', url = '/') => rgExec(reqUrl, rg__default['default'](url));
const getQueryParams = (url$1 = '/') => url.parse(url$1, true).query;
const matchParams = (path = '/', reqUrl = '/') => rg__default['default'](path).pattern.test(reqUrl);

exports.getQueryParams = getQueryParams;
exports.getURLParams = getURLParams;
exports.matchParams = matchParams;
exports.rgExec = rgExec;
